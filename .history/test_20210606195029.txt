void ex_gcd(int a, int b, int &x, int &y)
{
    if(b == 0) {x = 1; y = 0; return;}
    ex_gcd(b, a % b, x, y);
    int t = x; 
    x = y; 
    y = t - (a / b * y);
}

int main()
{
    read(a); read(b)
    ex_gcd(a, b, x, y); 
    x = (x % b + b) % b;
    write(x)
    return 0;
} 
//这段代码并没有什么实际的含义
int add(int x, int y)
{
    return x + y;
}

int gcd(int a, int b)
{
    if(!b)
    {
        return a;
    }
    return gcd(b, a % b);
}

int main()
{
    int n = 10, x, y, num[100];
    for(int i = 1; i <= n; i ++)
    {
        read(x); read(y);
        int tot = add(x, y);
        int g = gcd(x, y);
        write(tot); write(g);
		num[i] = tot + g;
    }
    return 0;
}